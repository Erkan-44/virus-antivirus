<Window x:Class="VirusAntivirus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VirusAntivirus"
        xmlns:converters="clr-namespace:VirusAntivirus.Converters"
        mc:Ignorable="d"
        Title="Virüs Antivirüs - Registry Tarayıcı" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <converters:SeverityColorConverter x:Key="SeverityColorConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20">
            <StackPanel>
                <TextBlock Text="🛡️ VİRÜS ANTİVİRÜS UYGULAMASI" 
                          FontSize="24" FontWeight="Bold" 
                          Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Registry Tabanlı Güvenlik Tarayıcısı" 
                          FontSize="14" Foreground="White" 
                          HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Threat List -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                    CornerRadius="5" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- List Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Tespit Edilen Tehditler" FontSize="16" FontWeight="Bold"/>
                        <TextBlock x:Name="ThreatCountText" Text=" (0)" FontSize="16" 
                                  Foreground="#2196F3" Margin="5,0,0,0"/>
                    </StackPanel>

                    <!-- Threat ListView -->
                    <ListView x:Name="ThreatListView" Grid.Row="1" 
                             SelectionChanged="ThreatListView_SelectionChanged"
                             BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Tehdit Tipi" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding ThreatType}" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding Severity, StringFormat='[{0}]'}" 
                                                          Foreground="{Binding Severity, Converter={StaticResource SeverityColorConverter}}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Kayıt Yolu" Width="300" DisplayMemberBinding="{Binding RegistryPath}"/>
                                <GridViewColumn Header="Anahtar" Width="150" DisplayMemberBinding="{Binding KeyName}"/>
                                <GridViewColumn Header="Değer" Width="250" DisplayMemberBinding="{Binding Value}"/>
                                <GridViewColumn Header="Açıklama" Width="200" DisplayMemberBinding="{Binding Description}"/>
                            </GridView>
                        </ListView.View>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>

                    <!-- Progress Bar -->
                    <ProgressBar x:Name="ScanProgressBar" Grid.Row="2" Height="5" 
                                Visibility="Collapsed" Margin="0,10,0,0"/>
                </Grid>
            </Border>

            <!-- Action Panel -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E0E0E0" 
                   BorderThickness="1" CornerRadius="5" Padding="15">
                <StackPanel>
                    <TextBlock Text="İşlemler" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

                    <!-- Scan Button -->
                    <Button x:Name="ScanButton" Content="🔍 Registry Taraması Başlat" 
                           Height="50" FontSize="14" FontWeight="Bold"
                           Background="#2196F3" Foreground="White"
                           BorderThickness="0" CornerRadius="5"
                           Click="ScanButton_Click" Margin="0,0,0,10"
                           Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#2196F3"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1976D2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Selected Threat Info -->
                    <Border BorderBrush="#E0E0E0" BorderThickness="1" 
                           CornerRadius="5" Padding="10" Margin="0,0,0,10"
                           Background="#FAFAFA">
                        <StackPanel>
                            <TextBlock Text="Seçili Tehdit Detayları" 
                                      FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBlock x:Name="SelectedThreatInfo" 
                                      TextWrapping="Wrap" Text="Bir tehdit seçin..."
                                      Foreground="#666"/>
                        </StackPanel>
                    </Border>

                    <!-- Action Buttons -->
                    <Button x:Name="RemoveButton" Content="🗑️ Tehdidi Kaldır" 
                           Height="40" FontSize="12"
                           Background="#F44336" Foreground="White"
                           BorderThickness="0" CornerRadius="5"
                           Click="RemoveButton_Click" Margin="0,0,0,10"
                           IsEnabled="False" Cursor="Hand"/>

                    <Button x:Name="RestoreButton" Content="↩️ Varsayılan Değere Döndür" 
                           Height="40" FontSize="12"
                           Background="#FF9800" Foreground="White"
                           BorderThickness="0" CornerRadius="5"
                           Click="RestoreButton_Click" Margin="0,0,0,10"
                           IsEnabled="False" Cursor="Hand"/>

                    <Button x:Name="ExportButton" Content="💾 Raporu Dışa Aktar" 
                           Height="40" FontSize="12"
                           Background="#4CAF50" Foreground="White"
                           BorderThickness="0" CornerRadius="5"
                           Click="ExportButton_Click" Margin="0,0,0,10"
                           Cursor="Hand"/>

                    <!-- Statistics -->
                    <Border BorderBrush="#E0E0E0" BorderThickness="1" 
                           CornerRadius="5" Padding="10" Margin="0,10,0,0"
                           Background="#FAFAFA">
                        <StackPanel>
                            <TextBlock Text="İstatistikler" FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBlock x:Name="StatsText" TextWrapping="Wrap" 
                                      Foreground="#666" FontSize="11"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#E0E0E0" Padding="10,5">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusText" Text="Hazır" FontSize="12"/>
                <TextBlock Text=" | " Margin="10,0"/>
                <TextBlock x:Name="LastScanText" Text="Henüz tarama yapılmadı" FontSize="12" Foreground="#666"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

